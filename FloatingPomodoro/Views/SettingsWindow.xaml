<Window x:Class="FloatingPomodoro.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FloatingPomodoro.Views"
        Style="{DynamicResource Dialog}" WindowStartupLocation="CenterScreen"
        Title="Settings" Width="400" SizeToContent="Height" Closed="Window_Closed">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="Foreground" Value="{DynamicResource FgBrush}"/>
        </Style>
        <Style TargetType="DockPanel">
            <Setter Property="Margin" Value="0,6"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="56"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Padding" Value="2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="150"/>
        </Style>
        <Style TargetType="Slider">
            <Setter Property="Width" Value="150"/>
            <Setter Property="IsSnapToTickEnabled" Value="True"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <TabControl MinHeight="300">
        <TabItem Header="Timer">
            <StackPanel Style="{StaticResource Pane}" Margin="14,8">
                <DockPanel><TextBox DockPanel.Dock="Right" Text="{Binding Settings.FocusMinutes, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Text="Pomodoro (minutes)"/></DockPanel>
                <DockPanel><TextBox DockPanel.Dock="Right" Text="{Binding Settings.ShortBreakMinutes, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Text="Short break (minutes)"/></DockPanel>
                <DockPanel><TextBox DockPanel.Dock="Right" Text="{Binding Settings.LongBreakMinutes, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Text="Long break (minutes)"/></DockPanel>
                <DockPanel><TextBox DockPanel.Dock="Right" Text="{Binding Settings.LongBreakInterval, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Text="Long break every (pomodoros)"/></DockPanel>
                <CheckBox Content="Auto-start focus sessions after a break" IsChecked="{Binding Settings.AutoStartFocus}" Margin="0,14,0,0"/>
                <CheckBox Content="Auto-start breaks after focus" IsChecked="{Binding Settings.AutoStartBreaks}"/>
                <TextBlock Style="{StaticResource Muted}" Text="Changes apply when this window closes." Margin="0,18,0,0" FontSize="11"/>
            </StackPanel>
        </TabItem>

        <TabItem Header="Appearance">
            <StackPanel Style="{StaticResource Pane}" Margin="14,8">
                <DockPanel>
                    <ComboBox DockPanel.Dock="Right" ItemsSource="{x:Static local:SettingsWindow.Themes}" SelectedItem="{Binding Settings.Theme}"/>
                    <TextBlock Text="Theme"/>
                </DockPanel>
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Width="40" TextAlignment="Right" Text="{Binding Value, ElementName=OpacitySlider, StringFormat={}{0:P0}}"/>
                    <Slider DockPanel.Dock="Right" x:Name="OpacitySlider" Minimum="0.7" Maximum="1" TickFrequency="0.05" Value="{Binding Settings.Opacity}"/>
                    <TextBlock Text="Window opacity"/>
                </DockPanel>
                <CheckBox Content="Show current task name" IsChecked="{Binding Settings.ShowTaskName}" Margin="0,14,0,0"/>
                <CheckBox Content="Show progress bar" IsChecked="{Binding Settings.ShowProgressBar}"/>
                <CheckBox Content="Always on top" IsChecked="{Binding Settings.AlwaysOnTop}"/>
                <CheckBox Content="Flash timer when a session ends" IsChecked="{Binding Settings.FlashOnFinish}"/>
            </StackPanel>
        </TabItem>

        <TabItem Header="Sound">
            <StackPanel Style="{StaticResource Pane}" Margin="14,8">
                <DockPanel>
                    <ComboBox DockPanel.Dock="Right" ItemsSource="{x:Static local:SettingsWindow.Sounds}" SelectedItem="{Binding Settings.Alarm}"/>
                    <TextBlock Text="Alarm"/>
                </DockPanel>
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Width="40" TextAlignment="Right" Text="{Binding Value, ElementName=VolumeSlider, StringFormat={}{0:P0}}"/>
                    <Slider DockPanel.Dock="Right" x:Name="VolumeSlider" Minimum="0" Maximum="1" TickFrequency="0.05" Value="{Binding Settings.AlarmVolume}"/>
                    <TextBlock Text="Volume"/>
                </DockPanel>
                <DockPanel>
                    <ComboBox DockPanel.Dock="Right" ItemsSource="{x:Static local:SettingsWindow.Repeats}" SelectedItem="{Binding Settings.AlarmRepeat}"/>
                    <TextBlock Text="Repeat"/>
                </DockPanel>
                <Button Content="Test alarm" Width="110" HorizontalAlignment="Left" Margin="0,14,0,0" Padding="6,4" Click="Test_Click"/>
                <CheckBox Content="Desktop notification when a session ends" IsChecked="{Binding Settings.DesktopNotifications}" Margin="0,16,0,0"/>
            </StackPanel>
        </TabItem>

        <TabItem Header="General">
            <StackPanel Style="{StaticResource Pane}" Margin="14,8">
                <CheckBox Content="Launch with Windows" IsChecked="{Binding Settings.LaunchAtStartup}" Margin="0"/>
                <CheckBox Content="Keep running in the tray when the timer is closed" IsChecked="{Binding Settings.MinimizeToTray}"/>
                <TextBlock Style="{StaticResource SectionLabel}" Text="KEYBOARD SHORTCUTS" Margin="0,18,0,6"/>
                <ItemsControl x:Name="Shortcuts">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Margin="0,1"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Style="{StaticResource Muted}" Text="Global. If a combo is taken by another app, Ctrl + Alt + Shift is tried instead." FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="Space · R · S · M · Esc  —  when the timer has focus" Margin="0,8,0,0"/>
                <TextBlock Style="{StaticResource SectionLabel}" Text="DATA" Margin="0,18,0,4"/>
                <TextBlock Style="{StaticResource Muted}" x:Name="DataPath" FontSize="11" TextWrapping="Wrap" Cursor="Hand" MouseLeftButtonUp="DataPath_Click" ToolTip="Open folder"/>
                <TextBlock Style="{StaticResource Muted}" x:Name="VersionText" FontSize="11" Margin="0,10,0,0"/>
            </StackPanel>
        </TabItem>
    </TabControl>
</Window>
