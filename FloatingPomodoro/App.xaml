<Application x:Class="FloatingPomodoro.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FloatingPomodoro"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <!-- Keys only. The palettes live in App.ApplyTheme, which fills these in before any window
             is created; putting colours here too would give one palette two homes, and only one is live. -->
        <SolidColorBrush x:Key="BgBrush"/>
        <SolidColorBrush x:Key="SurfaceBrush"/>
        <SolidColorBrush x:Key="FgBrush"/>
        <SolidColorBrush x:Key="Fg2Brush"/>
        <SolidColorBrush x:Key="AccentBrush"/>

        <local:BoolToVis x:Key="Vis"/>
        <local:BoolToVis x:Key="VisInv" Invert="True"/>

        <!-- Light text for a dialog's own content. This MUST stay keyed and be applied by hand:
             as a keyless implicit style it also matched the StackPanel that ComboBox uses as its
             items host inside its control template, which painted every dropdown row in this
             colour on Windows' light popup chrome and made the options invisible. Anything
             app-wide and inheritable leaks into popups the same way, so do not set text colour
             on Window either. SelfTest asserts no implicit Panel style comes back. -->
        <Style x:Key="Pane" TargetType="StackPanel">
            <Setter Property="TextElement.Foreground" Value="{DynamicResource FgBrush}"/>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Margin" Value="12"/>
            <Setter Property="Background" Value="{DynamicResource BgBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SurfaceBrush}"/>
        </Style>

        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource IconButton}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="22">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThinBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="PART_Track">
                            <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="2"/>
                            <Border x:Name="PART_Indicator" Background="{DynamicResource AccentBrush}" CornerRadius="2" HorizontalAlignment="Left"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dialog windows (settings / tasks / stats) -->
        <Style x:Key="Dialog" TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource BgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource FgBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="ResizeMode" Value="NoResize"/>
            <!-- WindowStartupLocation is not a dependency property: set it on each window, not here. -->
        </Style>
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Margin" Value="0,10,0,4"/>
        </Style>
        <Style x:Key="Muted" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Fg2Brush}"/>
        </Style>
    </Application.Resources>
</Application>
